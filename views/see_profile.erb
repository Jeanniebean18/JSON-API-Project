<h1> Hi <%= @owner.name %> </h1>

<!-- add new event by category -->

<h2>Add a new pet event:</h2>

<% Category.all.each do |category| %>
<p><a href = "/create_event/<%= category.id %>/<%= @owner.id %>"><%= category.name %> </a> 
  <% end %>

  <!-- listing pets to edit or delete -->
  <h2>Here are your pets:</h2>
 
  <!-- or each pet, show the corresponding events. -->
  <!-- or for each event, show details and pets involved. --> <!-- ## this will be the join table. -->
  <!-- both! -->
  <%pets = Pet.where("owner_id", @owner.id)%>
  
  <% pets.each do |pet| %>
  <b><p><h2> <%=pet.name%></h2></b> <a href="/edit_pet/<%=pet.id%>">edit name</a> <a href="/delete_pet/<%=pet.id%>">delete pet</a><%@petid = pet.id%></p>
  <%@petsandevents.each do |hash|%>
  <p><% if hash["pet"] == @petid%>
    <p><b><%= hash["name"]%></b> - date: <%= hash["date"]%> notes: <%= hash["comment"]%> - <%@event = Event.find(hash["id"]) %> due again: <%=@event.increment_date(@event.date, @event.reminder_time.to_i)%>
      <a href ="/delete_event_bridge/<%=@petid%>/<%=hash["id"]%>/<%=@owner.id%>"> delete from pet</a>
      <% end %>
      <% end %>
      <% end %>

      <!-- add pet -->
      <h2><a href="/add_pet/<%=@owner.id %>">Add a Pet</a></h2>
      <!-- edit user profile -->
      <p>Or, <a href="/edit_profile/<%= @owner.id %>">Edit your Profile</a> <a href="/home">Return to main menu?</a>
     
    
